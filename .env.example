# Lấy 2 giá trị này ở Supabase Dashboard > Project Settings > API.
# An toàn khi public (anon key bị giới hạn hoàn toàn bởi Row Level Security) —
# KHÔNG bao giờ đặt service role key ở đây / trong code frontend.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
